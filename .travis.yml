language: node_js

node_js:
  - '12'

services:
  - docker

git:
  quiet: true

cache:
  directories:
    - node_modules

branches:
  only:
    - /^dev\/.*$/
    - master

before_install:
  - sudo apt-get -y install sshpass

script:
  - ./cli/buildImage
  - ./cli/restartServer
